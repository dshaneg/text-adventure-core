'use strict';

import { expect } from 'chai';
import 'mocha';

describe('dummy test', () => {
  describe('Increment', () => {
    it('Should get 2 when adding 1 and 1.', () => {
      expect(1 + 1).to.equal(2);
    });
  });
});